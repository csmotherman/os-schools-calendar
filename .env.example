# Public Supabase connection values. Never commit service-role or database secrets.
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-key

# Local development value. Production must use the canonical HTTPS application origin.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
